@model IEnumerable<JobOffers.Models.JobModel>

@foreach (var gr in Model)
{
    <h3>@gr.JobTitle</h3>
    <table class="table table-bordered table-hover">
        <tr>
            <th>
                اسم الباحث
            </th>
            <th>
                تاريخ الارسال
            </th>
            <th>
                تاريخ الارسال
            </th>
        </tr>

        @foreach (var item in gr.Items)
        {
            <tr>

                <td>

                    @item.UserId

                </td>

                <td>
                    @item.Message
                </td>

                <td>
                    @item.ApplyDate
                </td>
            </tr>




        }

    </table>
}